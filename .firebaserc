{
  "projects": {
    "default": '${process.env.FIREBASE_PROJECT_ID}'
  },
  "targets": {
    '${process.env.FIREBASE_PROJECT_ID}': {
      "hosting": {
        "maggot": [
          '${process.env.FIREBASE_PROJECT_ID}'
        ]
      }
    }
  }
}